# Phase 0 Canonical Schema

## Purpose

This document defines the canonical schema shapes for the first two foundational model families:

- operational transaction header / line
- GL header / line

These are not meant to become one giant polymorphic production table. They are the reference contract that concrete business tables should follow.

Examples:

- `Invoice` should follow the transaction header contract
- `InvoiceLineItem` should follow the transaction line contract
- `JournalEntry` / future posting batches should follow the GL header contract
- `JournalEntryLineItem` / future posted lines should follow the GL line contract

The goal is to make future modules consistent, auditable, and performant.

## Design Rules

1. Use concrete tables per business object, not one universal transaction table.
2. Keep business transactions separate from accounting postings.
3. Make source lineage mandatory.
4. Make indexing part of the design.
5. Support dimensions, custom fields, open items, FX, and intercompany at the contract level.
6. Model journal adjustments and journal reversals so they are easy to reconcile and can participate cleanly in open-item management where applicable.

## Canonical Transaction Header Contract

### Required business identity fields

- `id`
- `recordType`
- `number`
- `businessId`
- `description`
- `status`

### Required date fields

- `transactionDate`
- `postingDate`
- `documentDate` where different from transaction date
- `dueDate` where relevant

### Required organizational fields

- `subsidiaryId`
- `locationId` where relevant
- `departmentId` where relevant
- `classId` where relevant
- `projectId` where relevant

### Required currency fields

- `transactionCurrencyId`
- `localCurrencyId`
- `functionalCurrencyId`
- `groupCurrencyId`
- `exchangeRate`
- `localExchangeRate`
- `functionalExchangeRate`
- `groupExchangeRate`

Notes:

- not every object needs every rate field persisted exactly this way
- but the contract should preserve enough to reconstruct entry and posting currency context

### Required workflow / control fields

- `approvalStatus`
- `postingStatus`
- `isPosted`
- `isVoided`
- `isReversed`

### Required source / linkage fields

- `sourceTransactionType` where derived from another process
- `sourceTransactionId`
- `sourceRunId` where generated by a run
- `externalReference`

### Transaction identity presentation rule

Every transaction family should align to this business-facing identity model:

- `DB Id`
- `Business Id`
- `Description`
- optional external / domain reference

Interpretation:

- `DB Id` maps to the true system id
- `Business Id` maps to the internal transaction number users work with
- `Description` maps to the business-facing narrative, usually memo for finance transactions

Display rule:

- detail pages should show all identity fields when they exist
- list pages and dropdowns should prefer:
  1. external / domain reference + description where that is the operational identifier
  2. otherwise business id + description

Examples:

- Bill: internal bill number as `Business Id`, vendor invoice number as external reference
- Sales Order: internal sales order number as `Business Id`, customer PO number as external reference
- Journal: journal number as `Business Id`, memo as `Description`

### Required party fields

Use the fields relevant to the transaction type:

- `customerId`
- `vendorId`
- `employeeId`
- `bankAccountId`

### Required narrative / metadata fields

- `memo`
- `reference`
- `notes`

### Required audit fields

- `createdAt`
- `updatedAt`
- `createdById`
- `updatedById`
- `approvedById`
- `postedById`
- `voidedById`
- `reversedById`

### Recommended indexes

- unique index on `number`
- index on `status`
- index on `transactionDate`
- index on `postingDate`
- index on `subsidiaryId`
- index on `transactionCurrencyId`
- index on `customerId` / `vendorId` / `employeeId` where applicable
- index on `sourceTransactionId`
- index on `sourceRunId`
- compound index on `(subsidiaryId, transactionDate)`
- compound index on `(postingStatus, postingDate)`

## Canonical Transaction Line Contract

### Required identity and ordering

- `id`
- `headerId`
- `lineNumber`
- `displayOrder`

### Required descriptive fields

- `description`
- `memo`

## Master Data Record Contract

Every master-data family should also follow a consistent identity baseline:

- `id`
- `businessId`
- `description`
- optional domain `number` / `code`

Interpretation:

- `DB Id` maps to `id`
- `Business Id` maps to the internal operational identifier controlled by Company Preferences / id settings where applicable
- `Description` maps to the human-readable record meaning
- optional domain `number` / `code` is shown when the business actually works from that identifier

Display rule:

- detail pages should show all identity fields when they exist
- list pages and dropdowns should prefer:
  1. number / code + description where available
  2. otherwise business id + description

Where a master-data family is numbered or coded, the `Business Id` should align with Company Preferences / id-settings behavior where applicable.

### Required quantity / pricing fields where relevant

- `quantity`
- `unitOfMeasureId`
- `unitPrice`
- `lineAmount`
- `taxAmount`
- `grossAmount`
- `netAmount`

### Required account / item fields where relevant

- `itemId`
- `accountId`
- `taxCodeId`

### Required dimension fields

- `departmentId`
- `classId`
- `locationId`
- `projectId`

### Required currency fields

- `transactionAmount`
- `localAmount`
- `functionalAmount`
- `groupAmount`

### Required open-item / posting flags where relevant

- `openItemEligible`
- `postingImpact`
- `lineStatus`

### Required source linkage

- `sourceLineType`
- `sourceLineId`

### Required audit fields

- `createdAt`
- `updatedAt`

### Recommended indexes

- index on `headerId`
- unique or compound index on `(headerId, lineNumber)`
- index on `itemId`
- index on `accountId`
- index on `projectId`
- compound index on `(headerId, displayOrder)`
- compound index on `(accountId, lineStatus)` where relevant

## Canonical GL Header Contract

### Purpose

Represents one posting event to the ledger.

### Journal adjustment and reversal principle

Manual and system-generated journal adjustments should be modeled so they help account reconciliation rather than obscure it.

That means journal architecture should support:

- explicit source and purpose classification
- clear reversal linkage
- open-item participation where the journal is intended to create, settle, adjust, or reopen an open item
- drill-through from reconciliation and roll-forward views back to the originating journal and line
- predictable activity typing for adjustment, reversal, accrual, deferral, reclass, and settlement-related journal activity

### Required identity fields

- `id`
- `journalNumber`
- `postingType`
- `postingStatus`

### Required date and period fields

- `postingDate`
- `accountingPeriodId`
- `fiscalYear`
- `fiscalPeriod`

### Required scope fields

- `subsidiaryId`
- `bookId` where multi-book is later enabled

### Required source lineage

- `sourceType`
- `sourceId`
- `sourceNumber`
- `sourceRunId`
- `reversesGlHeaderId`
- `reversalReasonCode`

### Required workflow / control fields

- `isReversal`
- `isAutoGenerated`
- `isIntercompany`
- `isOpenItemRelevant`

### Required narrative / metadata fields

- `memo`
- `reference`

### Required audit fields

- `createdAt`
- `updatedAt`
- `createdById`
- `postedById`
- `reversedById`

### Recommended indexes

- unique index on `journalNumber`
- index on `postingDate`
- index on `accountingPeriodId`
- index on `subsidiaryId`
- index on `sourceType`
- index on `sourceId`
- index on `sourceRunId`
- index on `reversesGlHeaderId`
- compound index on `(subsidiaryId, postingDate)`
- compound index on `(sourceType, sourceId)`

## Canonical GL Line Contract

### Purpose

Represents the debits and credits that hit the ledger.

### Reconciliation-friendly journal expectation

Journal lines should make reconciliation easy by preserving enough structured context to answer:

- what account movement is this line supporting?
- is it opening, operational, settlement, accrual, reversal, reclass, or FX-related?
- is it linked to an open item?
- is it reversing a prior line or offsetting one?
- what reconciliation or roll-forward bucket should it land in?

### Required identity and linkage

- `id`
- `glHeaderId`
- `lineNumber`
- `reversesGlLineId`

### Required accounting fields

- `accountId`
- `debitAmount`
- `creditAmount`
- `netAmount`

### Required currency fields

- `transactionCurrencyId`
- `localCurrencyId`
- `functionalCurrencyId`
- `groupCurrencyId`
- `transactionAmount`
- `localAmount`
- `functionalAmount`
- `groupAmount`
- `exchangeRateId` or equivalent FX source reference

### Required dimensional fields

- `departmentId`
- `classId`
- `locationId`
- `projectId`
- custom dimension references

### Required movement / analytical fields

- `activityType`
- `openItemId`
- `counterpartyType`
- `counterpartyId`
- `reconciliationGroupKey`

### Required source lineage

- `sourceTransactionId`
- `sourceTransactionLineId`
- `sourceScheduleLineId`
- `sourceApplicationId`
- `sourceRunDetailId`
- `reconciliationInstanceId`

### Required intercompany fields where relevant

- `intercompanyPairKey`
- `counterpartySubsidiaryId`
- `dueToFromIndicator`

### Required audit fields

- `createdAt`
- `updatedAt`

### Recommended indexes

- index on `glHeaderId`
- compound index on `(glHeaderId, lineNumber)`
- index on `accountId`
- index on `activityType`
- index on `reversesGlLineId`
- index on `openItemId`
- index on `sourceTransactionId`
- index on `sourceTransactionLineId`
- index on `counterpartyId`
- index on `intercompanyPairKey`
- compound index on `(accountId, postingDate)` through join or denormalized strategy where reporting requires it
- compound index on `(accountId, subsidiaryId, accountingPeriodId)` through join or denormalized strategy where reporting requires it

## Canonical Supporting Enumerations / Reference Sets

These should be standardized early, even if implemented as managed lists, enums, or reference tables later.

### Transaction status families

- draft
- pending approval
- approved
- processed / posted
- closed
- void
- reversed

### Posting status families

- unposted
- posted
- reversed
- failed

### Activity types

- opening_balance
- operational_movement
- settlement
- accrual
- deferral
- amortization
- revenue_recognition
- reclassification
- fx_remeasurement
- transfer
- writeoff
- reversal
- carry_forward

### Counterparty types

- customer
- vendor
- employee
- intercompany
- none

## Extension Hooks

The canonical schema must be compatible with:

- custom field values on headers and lines
- custom dimensions on transactions and GL
- open-item applications
- schedule-line posting
- run-generated transactions
- integration-generated transactions

## Retrofit Guidance Against Current App

Existing models do not need immediate destructive replacement.

Instead, use this document as the target contract for:

1. retrofitting current transaction families
2. shaping all new transaction families
3. designing shared posting services
4. deciding where to normalize current posting data further

Near-term retrofit candidates:

- `Invoice` / `InvoiceLineItem`
- `Bill` / `BillLineItem`
- `CashReceipt` / `CashReceiptApplication`
- `BillPayment` / `BillPaymentApplication`
- `JournalEntry` / `JournalEntryLineItem`

## Immediate Next Schema Tasks

After this document, the next design work should specify:

1. canonical custom field tables in detail
2. canonical custom dimension tables in detail
3. canonical open-item tables in detail
4. canonical FX and intercompany tables in detail
5. canonical run / integration tables in detail
